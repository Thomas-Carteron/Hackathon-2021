{% extends 'layout.html.twig' %}
{% block content %}
    <div class="container justify-content-center mt-5 d-flex justify-content-center ">
<table class="table table-dark w-50  p-3 border white ">
    <thead class="">
    <tr>
        <th class="text-center" scope="col">Top</th>
        <th class="text-center"  scope="col">Pseudo</th>
        <th class="text-center"  scope="col">Meilleur temps</th>
    </tr>
    </thead>
    {% for user in users  %}
    <tbody>
    <tr>
        <th  class="text-center" scope="row">{{ loop.index  }}</th>
        <td class="text-center" >{{ user.name }}</td>
        <td class="text-center" >{{ user.best_time }}s</td>
    </tr>
    {% endfor %}
    </tbody>
</table>
    </div>
{% endblock %}
